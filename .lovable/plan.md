# תיקון העלאת קבצי אודיו במסך התמלול

## הבעיה (אומתה בלוגים)

ה-Edge Function `upload-transcriber-file` מקבל את כל הקובץ ל-RAM, ואז בונה גוף multipart שמכפיל את הבייטים פעם נוספת. עבור MP3/WAV/M4A טיפוסיים זה חורג מתקרת ותבדוק שזה עובד לכל היוזרים